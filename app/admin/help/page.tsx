import Link from 'next/link';
import type { ReactNode } from 'react';

export const metadata = { title: 'Инструкция' };

export default function HelpPage() {
  return (
    <div className="max-w-4xl space-y-8 pb-16">
      <div>
        <h1 className="text-2xl font-semibold">Инструкция по платформе</h1>
        <p className="mt-1 text-sm text-gray-600">
          Как вести базу компаний, отвечать клиентам и не потерять данные. Держите вкладку под рукой.
        </p>
      </div>

      <Section title="Главное правило: два мира данных">
        <p>
          В платформе есть <b>закрытые данные</b> (телефон продавца, цена закупа) и{' '}
          <b>открытые</b> (название, регион, год, налоговый режим, обороты, цена продажи). Выгрузка
          «без контактов и цен закупа» и карточки, которые бот показывает партнёрам, собираются
          только из второго набора. Клиентам с Avito бот дополнительно проверяет каждое сообщение
          на телефоны, ссылки и цены перед отправкой.
        </p>
        <Warn>
          Не вставляйте вручную названия и ИНН в переписку с покупателем — это единственный способ
          обойти защиту. Общайтесь номерами карточек.
        </Warn>
      </Section>

      <Section title="Статусы компаний — от них зависит всё">
        <Table
          head={['Статус', 'Что означает', 'Видит ли клиент']}
          rows={[
            ['draft', 'черновик: данные не проверены', 'нет'],
            ['verified', 'проверено вами, готово к продаже', 'ДА — попадает в списки и подбор бота'],
            ['reserved', 'зарезервирована под сделку', 'нет'],
            ['sold', 'продана', 'нет'],
            ['archived', 'неактуальна (ликвидирована и т.п.)', 'нет'],
          ]}
        />
        <p>
          Перевод <code>draft → verified</code> — ваше главное действие. Пока карточка в черновике,
          она не существует для клиентов. Карточки, созданные ботом, всегда приходят черновиками.
        </p>
        <Warn>
          Удаления нет. Кнопка «В архив» меняет статус на <code>archived</code> — история и ссылки
          сохраняются.
        </Warn>
      </Section>

      <Section title="Компании: поиск и фильтры">
        <ul className="list-disc space-y-1 pl-5">
          <li>
            <b>Поиск</b> понимает три вещи: ID карточки (<code>223</code>, <code>#223</code>),
            название и ИНН.
          </li>
          <li>
            <b>Обороты</b> и <b>цена</b> задаются диапазоном «от — до». Обороты в млн ₽, цена в
            тыс ₽ (как в исходной таблице: 100 = 100 000 ₽).
          </li>
          <li>
            <b>Требует проверки</b> — карточки, где что-то не распозналось при импорте. Их надо
            просмотреть глазами.
          </li>
        </ul>
        <Warn>
          Фильтры по оборотам и цене отсекают карточки с пустым значением. Если цена записана как
          «Дог» или обороты только текстом («ежегодно 1,5–3 млн») — число не извлеклось, и под
          фильтр такая карточка не попадёт. Чтобы увидеть все — очистите поля диапазонов.
        </Warn>
        <p>
          Фильтры действуют сразу на всё: таблицу, экспорты, список для клиента и массовые операции.
        </p>
      </Section>

      <Section title="Массовая смена статуса">
        <ol className="list-decimal space-y-1 pl-5">
          <li>Отметьте строки галочками (в шапке есть «выделить все на странице») → «Применить к выбранным».</li>
          <li>
            Либо жёлтая панель под таблицей — <b>«Применить ко всем N»</b>: действует на весь
            результат фильтра, включая другие страницы.
          </li>
        </ol>
        <Warn>
          «Применить ко всем» — самая опасная кнопка: она затрагивает записи, которых вы не видите
          на экране. Всегда сверяйте число N с числом компаний в заголовке.
        </Warn>
      </Section>

      <Section title="Отправка списка клиенту">
        <p>
          Бирюзовый блок на странице «Компании». Список строится по текущим фильтрам и{' '}
          <b>всегда только из verified</b> — черновик не «утечёт», даже если он в фильтре.
        </p>
        <ul className="list-disc space-y-1 pl-5">
          <li>
            <b>Текст для чата</b> — копируете и вставляете в Avito. Каждая позиция помечена номером
            карточки: «№ 223 — Татарстан, регистрация 2021, УСН 6%, цена обсуждается».
          </li>
          <li>
            <b>Скачать .xlsx</b> — тот же список файлом, если удобнее приложить таблицу.
          </li>
          <li>
            <b>Расшифровка номеров</b> — только для вас: какой номер какой компании соответствует.
          </li>
          <li>
            <b>Записать в историю выдач</b> — необязательно, но полезно: сохраняет, кому и что
            отправили (раздел «Выдачи»).
          </li>
        </ul>
        <p>
          Клиент отвечает «интересует 223» → вбиваете <code>223</code> в поиск или открываете
          адрес <code>/admin/companies/223</code>. Номер не меняется никогда.
        </p>
      </Section>

      <Section title="Пополнение базы: три способа">
        <Table
          head={['Способ', 'Когда использовать', 'Что получается']}
          rows={[
            ['Импорт (.xlsx)', 'большой файл от партнёра', 'предпросмотр diff → «Применить»'],
            ['Из сообщения', 'продавец написал в чат', 'текст → разбор → проверка → сохранение'],
            ['+ компания', 'одна фирма вручную', 'пустая карточка, заполняете сами'],
          ]}
        />
        <p>
          <b>Импорт</b> двухфазный: сначала предпросмотр (что добавится, что обновится, где
          конфликты), потом «Применить». До нажатия в базе ничего не меняется.
        </p>
        <Warn>
          Правило «импорт не стирает»: пустая ячейка в файле <b>не затирает</b> заполненное поле в
          базе. Чтобы очистить поле — правьте карточку руками.
        </Warn>
        <p>
          <b>Из сообщения</b>: вставляете переписку целиком, платформа сама вытаскивает данные и
          показывает, новая это компания или обновление существующей. Телефоны, e-mail и ИНН
          маскируются до отправки в нейросеть — она их не видит.
        </p>
      </Section>

      <Section title="Проверка по ЕГРЮЛ">
        <p>
          В карточке — блок «Данные ЕГРЮЛ» и кнопка «Проверить по ИНН» (данные ФНС через DaData):
          официальное название, статус, дата регистрации, ОКВЭД, юридический адрес. Если год не
          совпадает с тем, что сказал продавец, платформа это подсветит.
        </p>
        <p>
          В таблице компаний есть колонка «ЕГРЮЛ»: зелёный — действующая, оранжевый — ликвидируется,
          красный — ликвидирована.
        </p>
        <Warn>
          Обороты и лицензии в бесплатном тарифе DaData не приходят. Для них — кнопки под блоком:
          <b> ГИР БО</b> (выручка по годам), <b>Прозрачный бизнес</b> (налоги, долги, численность),
          <b> Федресурс</b> (банкротства). ИНН подставляется автоматически, проверка ручная.
        </Warn>
      </Section>

      <Section title="Бот Avito">
        <p>Работает по жёстким правилам, которые нельзя нарушить даже случайно:</p>
        <ul className="list-disc space-y-1 pl-5">
          <li><b>Максимум 2 сообщения</b> на диалог — дальше молчит (экономия на платных чатах).</li>
          <li><b>Никаких</b> ссылок, телефонов, адресов и конкретных цен — только человек.</li>
          <li>Не хватает данных — не переспрашивает, а передаёт вам.</li>
          <li>Отвечает только по разрешённым объявлениям (сейчас — «Продам, выкуплю ООО»).</li>
        </ul>
        <p>
          Продавец с полными данными → карточка-черновик + лид + сигнал в Telegram. Покупатель →
          до 3 обезличенных вариантов и передача вам.
        </p>
        <Warn>
          Если включён встроенный «Ассистент Авито», отключите его в кабинете Avito — иначе клиент
          получит два автоответа подряд.
        </Warn>
      </Section>

      <Section title="Разделы">
        <Table
          head={['Раздел', 'Зачем']}
          rows={[
            ['Дашборд', 'счётчики и последние эскалации'],
            ['Компании', 'база, фильтры, выгрузка в Excel, проверка ЕГРЮЛ, массовые операции'],
            ['Из сообщения', 'быстрое добавление из переписки'],
            ['Импорт', 'загрузка .xlsx с предпросмотром'],
            ['Выдачи', 'кому и что отправляли'],
            ['Лиды', 'обращения от бота, отметка «обработан»'],
            ['Диалоги', 'переписка, что бот извлёк, сколько сообщений потрачено'],
          ]}
        />
      </Section>

      <Section title="Что важно помнить">
        <ul className="list-disc space-y-1 pl-5">
          <li>Каждая правка пишется в историю карточки — видно, кто и когда менял.</li>
          <li>Клиенту показываются только <code>verified</code>. Забыли перевести — фирма не продаётся.</li>
          <li>Номер карточки (ID) — единая валюта общения: и с клиентом, и внутри команды.</li>
          <li>Проверяйте статус ЕГРЮЛ перед переводом в <code>verified</code>: ликвидирующаяся фирма в продаже — репутационный риск.</li>
        </ul>
      </Section>

      <p className="text-sm text-gray-500">
        Вопросы и доработки — через разработчика.{' '}
        <Link href="/admin" className="text-blue-600 underline">
          На дашборд
        </Link>
      </p>
    </div>
  );
}

function Section({ title, children }: { title: string; children: ReactNode }) {
  return (
    <section className="space-y-2">
      <h2 className="border-b pb-1 text-lg font-medium">{title}</h2>
      <div className="space-y-2 text-sm leading-relaxed text-gray-800">{children}</div>
    </section>
  );
}

function Warn({ children }: { children: ReactNode }) {
  return (
    <p className="rounded border border-amber-200 bg-amber-50 p-2 text-sm text-amber-900">
      ⚠ {children}
    </p>
  );
}

function Table({ head, rows }: { head: string[]; rows: string[][] }) {
  return (
    <div className="overflow-x-auto rounded border bg-white">
      <table className="w-full text-sm">
        <thead className="bg-gray-100 text-left text-xs uppercase text-gray-600">
          <tr>
            {head.map((h) => (
              <th key={h} className="px-3 py-2">{h}</th>
            ))}
          </tr>
        </thead>
        <tbody className="divide-y">
          {rows.map((r, i) => (
            <tr key={i}>
              {r.map((cell, j) => (
                <td key={j} className={`px-3 py-2 ${j === 0 ? 'font-medium' : ''}`}>{cell}</td>
              ))}
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
